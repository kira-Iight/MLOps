# Разбор четырёх дефектов

## Дефект 1: Промпт не замаскирован в лоссе

**В чём был.** `mask_prompt` возвращала `list(input_ids)` без изменений, а `n_prompt` в `encode_example` был жёстко 0. То есть `labels` совпадали с `input_ids` — модель училась предсказывать и промпт, и ответ.

**Как проявлялся.** Проверка 1 падала: под `label != -100` декодировался не ответ ассистента, а кусок промпта (`system\nТы помогаешь редактору...`). Обученная модель потом повторяла формулировку вопроса вместо ответа — потому что именно этому её и учили.

**Как исправлен.** В `mask_prompt` добавлено `[LABEL_PAD_ID] * n_prompt + list(input_ids[n_prompt:])`. В `encode_example` граница промпта вычисляется через `prompt_token_len` — токены промпта как префикс токенов полного текста, запасной путь по символьным офсетам при склейке BPE.

**Числом.** До: `n_prompt = 0`, под маской весь пример. После: `supervised_tokens = 17067` из `total_tokens = 668263` (train), то есть **2.55% токенов в лосс** — промпт занимает ~97.5%. На курсовом срезе лекция даёт 9.1% — у нас меньше, потому что новостные тексты длиннее, а ответ — короткая метка класса.

## Дефект 2: Шаблон обучения и инференса разошлись

**В чём был.** `build_chat_text` для `add_generation_prompt=False` собирала строку вручную через `TURN = "<|im_start|>{role}\n{content}<|im_end|>\n"`. Для `True` использовался `apply_chat_template`. Из-за этого train-строка не содержала `<think></think>`-блок, который Qwen3 (при `enable_thinking=False`) сам подставляет в промпт.

**Как проявлялся.** Проверка 2 падала на байте 572: inference-путь содержал `<think>\n\n</think>`, train-путь — сразу `Категория: sports`. Модель училась на одном формате, а в проде получала другой. Никакой лог этого не показывал.

**Как исправлен.** Оба пути идут через `tokenizer.apply_chat_template`. Константа `TURN` удалена. Строка инференса теперь побайтовый префикс строки обучения по построению, а не по совпадению.

**Числом.** До: расхождение на байте 572. После: проверка 2 сообщает «сверено диалогов: 304», проверка 3 — «спецтокенов ролей в src/ нет».

## Дефект 3: Паддинг справа вместо левого

**В чём был.** `tokenize.padding_side` из `params.yaml` не передавался в токенизатор (`# TODO: tokenize.padding_side из params.yaml сюда так и не доехал`). У `DynamicPaddingCollator` значение по умолчанию было `"right"`.

**Как проявлялся.** Проверка 4 падала: `metrics/tokenize.json` содержал `padding_side = 'right'`. При batch > 1 в decoder-only модели правое дополнение встаёт между промптом и первым сгенерированным токеном — генерация начинается с мусора. На batch = 1 это незаметно, поэтому дефект тихий: «у меня всё работает» ничего не доказывает.

**Как исправлен.** В `main()` добавлено `tokenizer.padding_side = params["tokenize"]["padding_side"]` и `pad_token = eos_token` если пусто. В `DynamicPaddingCollator` default изменён на `"left"`.

**Числом.** До: `padding_side = 'right'` в метриках и коллаторе. После: `padding_side = 'left'` в params, metrics и сохранённом blob. Проверка 4 — зелёная.

## Дефект 4: Обрезка молчаливая

**В чём был.** `truncation_stats` возвращала пустой словарь. `process_split` вызывала её, но результат был `{}` — в метриках не было ни `truncated`, ни `truncated_ratio`.

**Как проявлялся.** Проверка 5 падала: «в метриках нет доли обрезанных примеров — обрезка молчаливая». В `docs/tokenize_report.md` ячейка «обрезано» показывала `НЕ СЧИТАЛАСЬ`. При исходном `max_seq_len: 232` доля обрезки для новостного датасета — почти 100%, и никто об этом не узнавал.

**Как исправлен.** `truncation_stats` считает `truncated` и `truncated_ratio` по всем записям сплита, сравнивает с `tokenize.truncated_warn_ratio` и печатает предупреждение при превышении. Плюс `max_seq_len` поднят с 232 до **1024** — цифра выбрана по распределению длин в `docs/tokenize_report.md` (p99 = 898, max = 1062), а не «чтобы позеленело».

**Числом.** До: ключей `truncated*` в метриках не было. После: `truncated_ratio = 0.0009` (train, 2 примера) и `0.0` (val) при пороге 5% — проверка 5 зелёная.